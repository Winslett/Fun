%h1 My Users

%ul
  - @users.each do |user|
    %li
      = user["name"]
      %ul
        - user.each do |attribute, value|
          - next if attribute == "auth_key"
          %li
            = attribute
            \:
            %b= value
